final class Values {
  static const double radiusValue = 10;
  static const double radiusCircleValue = 6888;

  static const Duration days = Duration(days: 1);
  static const Duration hours = Duration(hours: 1);
  static const Duration minutes = Duration(minutes: 1);
  static const Duration seconds = Duration(seconds: 1);
  static const Duration milliseconds = Duration(milliseconds: 100);
}
